info:
  name: crear videojuego
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:3000/videojuegos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
