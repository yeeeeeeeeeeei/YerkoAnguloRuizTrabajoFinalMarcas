info:
  name: Buscar por nombre
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/videojuegos/buscar/nombre?nombre=resident
  params:
    - name: nombre
      value: resident
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
