info:
  name: reseñas por videojuego
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/resenas/videojuego/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
