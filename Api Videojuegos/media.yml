info:
  name: media
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:3000/videojuegos/stats/media
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
