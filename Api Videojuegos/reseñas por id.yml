info:
  name: reseñas por id
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/resenas/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
