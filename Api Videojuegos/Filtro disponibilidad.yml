info:
  name: Filtro disponibilidad
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3000/videojuegos/filtrar/disponible?disponible=true
  params:
    - name: disponible
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
